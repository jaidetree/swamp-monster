# Python
__pycache__/
*.pyc
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# uv
/uv.lock.tmp

# Django
/staticfiles/
/media/
*.log
.env

# Tailwind build output (source lives in static_src/, this is generated)
/static/css/app.css

# Node
npm-debug.log
node_modules/

# Local Postgres data (scripts/db)
postgres_data/
.pgdata/
.pgsocket/

# Playwright
/test-results/
/playwright-report/

# Obsidian workspace state (local UI state, not project content).
.swamp-vault/.obsidian/workspace*

# Stale Elixir artifacts (repo was migrated off Phoenix, see db69f21)
/_build/
/deps/
/priv/
erl_crash.dump
.elixir-tools/
.elixir_ls/
.lexical/

# Legacy Elixir reference worktree (parity reference during the Django port,
# not tracked in this repo's history — see legacy-elixir branch)
/legacy-elixir/

# Obsidian local UI state at repo root
/.obsidian/
